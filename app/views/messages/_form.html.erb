<%= simple_form_for [connection, message], remote: true, url: "#{connection_messages_path(@connection)}#message_field" do |f| %>
  <div class="d-flex justify-content-between align-items-center">
    <%= f.input :content, label: false, class: 'form-control' %>
    <%= f.submit 'send',  class: 'btn btn-primary btn-sm text-white' %>
  </div>
  <% end %>
<!-- variable.model_name + "_" variable2.model_name + "_path" -->
