<div class="container">
  <div>
    <%= link_to connections_path, class: "nav-link m-0 px-0 py-3" do %>
      <i class="fas fa-long-arrow-alt-left"> Back to your buddies</i>
    <% end %>
  </div>
  <div class="profile-buddyup-header pb-5">
    <div class="profile-header-buddy-score">
      <h6><%= @connection.score %>%</h6>
      <p>MATCH</p>
    </div>
    <img src="<%= @buddy.photo %>" class="avatar-large mb-3"/>
    <div class="profile-header-infos">
      <h3><%= @buddy.first_name %>, <%= @buddy.age %></h3>
      <h6><%= @buddy.city %></h6>
      <p><%= @buddy.intro %></p>
    </div>
  </div>
  <hr class="mt-4 mb-2">
  <div class="card-body py-2">
    <p>We've highlighted shared interests between you and <%= @buddy.first_name %> for you!</p>
    <% @questions.each do |question| %>
      <% unless question.id == 9 %>
        <div class="question mt-3">
          <% responses = question.responses.map do |response| %>
            <% response.answer %>
          <% end %>
          <% overlap = responses & @buddy_user_responses %>
          <% unless overlap.empty? %>
            <h5 class="px-1 m-0"><%= question.description %></h5>
            <div class="pt-2">
              <% overlap.each do |i| %>
                <% if @current_user_responses.include?(i) %>
                  <button class="btn btn-primary btn-sm round mb-2 mr-1 p-2"><%= i %></button>
                <% else %>
                  <button class="btn btn-outline-primary btn-sm round mb-2 mr-1 p-2"><%= i %></button>
                <% end %>
              <% end %>
            </div>
          <% end %>
        </div>
      <% end %>
    <% end %>
  </div>
  <hr class="mt-2 mb-2">
  <div class="d-flex justify-content-center m-0 p-3">
    <a href="#" class="bu-brand-button bu-button-lg">Chat with <%= @buddy.first_name %></a>
  </div>
</div>
